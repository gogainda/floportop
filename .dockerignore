# Default deny-all; include only runtime build inputs.
*

# Build metadata.
!.dockerignore
!deploy/
!deploy/docker/
!deploy/docker/Dockerfile

# Dependency lockfiles.
!requirements/
!requirements/prod.lock

# Runtime application source.
!apps/
!apps/**
!src/
!src/**
!start.sh

# Model artifacts required at runtime.
!models/
!models/**

# Exclude large artifacts that are fetched during image build.
models/*.faiss
models/movies.pkl
models/*.parquet

# Large ML artifacts (downloaded at runtime or from GCS)
cache/
*.faiss
*.bin
*.pt
*.pth

# Jupyter & Data
.ipynb_checkpoints
data/
notebooks/

# Mac/System junk
.DS_Store
Thumbs.db

# Deployment artifacts
Dockerfile
.dockerignore

# Dev files not needed in production
requirements_dev.txt
requirements.txt
Makefile
README.md
scripts/
*.egg-info/
.ruff_cache/
.envrc
.python-version
setup.py
.vscode/
.idea/
